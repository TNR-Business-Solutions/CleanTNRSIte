# 🚀 Server Running on Port 8000

## Configuration Updated

The server has been configured to run on **port 8000** instead of 3000.

### Access URLs:
- **Admin Login**: http://localhost:8000/admin-login.html
- **Admin Dashboard**: http://localhost:8000/admin-dashboard-v2.html
- **Home Page**: http://localhost:8000/

### Login Credentials:
- **Username**: `admin`
- **Password**: `TNR2024!`

### Configuration:
The `.env` file has been updated with:
```
PORT=8000
```

### To Start Server:
```bash
npm start
```
OR
```bash
node serve-clean.js
```

The server will automatically use port 8000 from the `.env` file.

---

**Server should be starting on port 8000 now!**
