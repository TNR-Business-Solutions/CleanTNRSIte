// Dedicated Admin Auth Endpoint
// This file ensures the route works even if catch-all routing has issues

const handler = require('../../server/handlers/admin-auth');

module.exports = handler;

